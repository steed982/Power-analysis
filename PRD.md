# PRD - 电池摄像头功耗计算器（项目版 Web）

## 1. 产品概述

### 1.1 背景

研发/FAE 在方案评估阶段需要快速估算不同电池、无线类型、硬件平台和工作模式下的续航表现。现有参考页面（`LPGE`）可计算，但以“单页场景”方式为主，缺少项目化保存与平台规则约束。

### 1.2 产品目标

提供一个网页工具，支持：

- 按“项目”保存输入配置
- 结合平台/无线规则自动约束参数
- 快速输出续航与功耗结果
- 支持导出用于评审沟通

### 1.3 目标用户

- 内部研发工程师
- FAE / 技术支持

## 2. 功能范围

### 2.1 In Scope（当前版本）

- 项目管理（新建/保存/另存为/删除/下拉选择）
- 项目参数配置（电池电量、无线类型、硬件平台）
- 4 种功耗模式计算（受平台规则约束）
- 实时结果展示与图表
- CSV 导出、打印/PDF
- 本地存储与旧版数据迁移

### 2.2 Out of Scope（后续版本）

- 云端同步/账户体系
- 多人协作
- PNG 导出
- 实测数据拟合/模型校准
- BOM/器件数据库联动

## 3. 核心业务规则

### 3.1 硬件平台与模式限制

- `OV8000`：仅支持
  - `休眠唤醒`
  - `长电模式`
- 其他平台（`6920`、`6921`、`6941`、`其他`）：
  - 支持全部 4 种模式（休眠唤醒/AOR/长电/智能）

### 3.2 平台切换自动回退

- 当切换平台导致当前模式非法时：
  - 自动切换为 `休眠唤醒`
  - 在输入校验区显示提示信息

### 3.3 无线类型与休眠功耗绑定

- `Wi-Fi` -> 休眠功耗固定为 `2mW`
- `4G` -> 休眠功耗固定为 `6mW`
- `休眠功耗` 字段只读显示，不允许手动编辑

## 4. 用户流程

### 4.1 新建项目并计算

1. 点击“新建”
2. 填写项目名称
3. 选择电池电量、无线类型、硬件平台
4. 选择功耗模式
5. 输入功耗参数
6. 查看实时结果与图表
7. 自动保存（也可点击“保存项目”）

### 4.2 切换已保存项目

1. 在项目下拉框选择项目
2. 页面回填该项目全部参数
3. 自动显示对应模式/规则与计算结果

### 4.3 导出数据

1. 点击“导出 CSV”导出全部项目数据
2. 点击“打印/PDF”生成评审输出

## 5. 页面与模块需求

## 5.1 顶部区域

- 标题与说明
- 操作按钮：
  - 保存项目
  - 另存为
  - 导出 CSV
  - 打印/PDF

## 5.2 左侧配置区（侧栏）

- 项目管理模块
  - 项目下拉
  - 新建
  - 删除当前项目
  - 保存状态提示
- 模式选择模块
  - 4 个模式按钮
  - 平台规则提示文案
- 表单模块
  - 项目参数配置
  - 基础硬件配置
  - 活动功耗配置
  - 待机配置
- 输入校验模块
  - 错误 / 警告 / 规则提示 / 正常提示

## 5.3 右侧结果区

- 续航主卡（天/小时、月）
- 指标卡（平均功耗、每日能耗、活动时长）
- 能耗构成图表

## 6. 关键计算指标

- `续航天数`
- `续航月数`
- `平均功耗 (mW)`
- `每日能耗 (mWh)`
- `活动时长/天`
- `能耗构成（活动/休眠/AOR）`

## 7. 验收标准（UAT）

### 7.1 规则验证

- 切换 `Wi-Fi/4G` 时休眠功耗正确自动变为 `2/6`
- 选择 `OV8000` 时 `AOR/智能` 模式不可选
- 从 `智能` 切换到 `OV8000` 会自动回退到 `休眠唤醒`

### 7.2 项目管理验证

- 新建项目后出现在下拉中
- 修改参数刷新页面后仍保留
- 另存为不覆盖原项目
- 删除项目后自动切换到其它项目或创建默认项目

### 7.3 结果输出验证

- 图表与结果卡数值一致
- CSV 导出包含项目字段（平台、无线类型、模式等）
- 打印/PDF 页面可正常输出

## 8. 非功能需求

- 响应式可用（桌面/手机）
- 表单控件触控尺寸 >= 44px
- 可见 focus 态
- 本地运行，无后端依赖

## 9. 版本信息

- 当前实现定位：MVP / Web 项目版
- 数据存储版本：`v2`（兼容迁移 `v1`）
